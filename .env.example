GROQ_API_KEY=your_key_here
PORT=5000
APP_PASSWORD=your_password_here
# Optional: separate key used only to sign session cookies. If unset,
# APP_PASSWORD is reused for this — setting your own is slightly safer.
SESSION_SECRET=
